name: roboshop

services:
  mongodb:
    build: ./mongodb
    image: mongodb:v1
    container_name: mongodb

  catalogue:
    build: ./catalogue
    image: catalogue:v1
    container_name: catalogue
    depends_on:
      - mongodb

  FRONTEND:
    build: ./frontend
    image: frontend:v1
    container_name: frontend
    ports:
      - "81:80"
    depends_on:
      - catalogue

networks:
  default:
    name: roboshop
    driver: bridge